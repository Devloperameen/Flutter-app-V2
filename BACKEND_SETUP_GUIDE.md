# 🚀 FitFlow - Complete Backend Setup Guide\n\n**Status:** Backend fixed and ready to use  \n**Date:** August 12, 2026  \n\n---\n\n## ⚡ Quick Start\n\n### For Development (Local Backend)\n\n```bash\n# 1. Start backend server\ncd /home/sadiq/FlutterProjects/fitflow_gym/backend\nnpm install  # Only needed once\nnpm run dev\n\n# Backend is now running on http://localhost:5000/api/v1\n\n# 2. In another terminal, run Flutter app\ncd /home/sadiq/FlutterProjects/fitflow_gym\nflutter clean\nflutter pub get\nflutter run\n\n# App will connect to http://localhost:5000/api/v1 ✅\n```\n\n---\n\n## 🔧 Backend Configuration\n\n### Current Setup\n\n**File:** `/home/sadiq/FlutterProjects/fitflow_gym/backend/.env`\n\n```env\nNODE_ENV=development\nPORT=5000\nAPI_VERSION=v1\nMONGODB_URI=mongodb+srv://habittracking:G1HDEewgMJXCo5qn@habittrucking.rjzolku.mongodb.net/fitflow\nJWT_SECRET=dev-jwt-secret-key-for-testing-only-change-in-production\nJWT_REFRESH_SECRET=dev-refresh-secret-key-for-testing-only-change-in-production\nJWT_ACCESS_EXPIRY=15m\nJWT_REFRESH_EXPIRY=7d\nLOG_LEVEL=info\nCORS_ORIGIN=*\n```\n\n**Status:** ✅ All configured correctly\n\n---\n\n## 📱 App Configuration\n\n### Updated API Base URL\n\n**File:** `/home/sadiq/FlutterProjects/fitflow_gym/lib/core/network/api_endpoints.dart`\n\n```dart\n// ✅ FIXED: Changed from Render to local backend\nstatic const String baseUrl = 'http://localhost:5000/api/v1';\n```\n\n**What was changed:**\n- ❌ Before: `https://flutter-app-v2.onrender.com/api/v1` (Render - was down)\n- ✅ After: `http://localhost:5000/api/v1` (Local backend)\n\n---\n\n## 🏃 Running the Full Stack\n\n### Step 1: Start Backend\n\n```bash\ncd backend\nnpm run dev\n```\n\n**Expected output:**\n```\n✅ Socket.IO configured with WebSocket support\n✅ Socket.IO event handlers registered\n✅ Helmet security headers enabled\n✅ CORS configured\n✅ Rate limiting enabled\n✅ Body parser middleware enabled\n✅ Static file serving enabled at /uploads\n✅ All routes registered\n✅ Database connected: ac-8jkwhma-shard-00-02.rjzolku.mongodb.net\n✅ Server running on http://localhost:5000\n```\n\n### Step 2: Start Flutter App\n\n**For Emulator:**\n```bash\nflutter run\n```\n\n**For Physical Device:**\n```bash\n# Make sure device is connected\nadb devices\n\n# Run app\nflutter run\n```\n\n---\n\n## ✅ Verify Everything Works\n\n### Test 1: Backend is responding\n\n```bash\ncurl -X GET http://localhost:5000/api/v1/auth/me \\\n  -H \"Authorization: Bearer YOUR_JWT_TOKEN\"\n```\n\n**Expected:** User object (if authenticated)\n\n### Test 2: Focus Timer\n\n1. Open app\n2. Go to Focus Timer\n3. Click \"25 Minutes\"\n4. **Expected:** Timer starts and counts down ✅\n5. **If fails:** Check backend `/focus` endpoints\n\n### Test 3: Community Posts\n\n1. Open app\n2. Go to Community\n3. **Expected:** Posts load and display ✅\n4. Type message and send\n5. **Expected:** Message appears in real-time ✅\n6. **If fails:** Check Socket.IO connection\n\n### Test 4: Profile Analytics\n\n1. Open app\n2. Go to Profile\n3. **Expected:** Shows real rank (not #12) ✅\n4. **Expected:** Shows real focus hours ✅\n5. **Expected:** Shows real streak days ✅\n6. **If fails:** Check `/analytics/my-rank` endpoint\n\n### Test 5: Dashboard\n\n1. Open app\n2. Go to Home\n3. **Expected:** Shows real user data ✅\n4. **Expected:** Shows real daily quote ✅\n5. **If fails:** Check `/dashboard` endpoint\n\n---\n\n## 🐛 Troubleshooting\n\n### Backend won't start\n\n**Error:** `EADDRINUSE: address already in use :::5000`\n\n**Solution:** Another process is using port 5000\n\n```bash\n# Kill the process\nlsof -ti:5000 | xargs kill -9\n\n# Or use different port\nPORT=5001 npm run dev\n\n# Then update app to use port 5001:\n# static const String baseUrl = 'http://localhost:5001/api/v1';\n```\n\n### MongoDB connection fails\n\n**Error:** `Failed to connect to MongoDB`\n\n**Solution:** Check internet connection (Atlas is cloud-hosted)\n\n```bash\n# Test connection\nping habittrucking.rjzolku.mongodb.net\n\n# If fails: May need to allow IP in MongoDB Atlas\n# Go to: https://cloud.mongodb.com → Network Access → Add Current IP\n```\n\n### Flutter app can't reach backend\n\n**Error:** `Failed host lookup: 'localhost'`\n\n**Cause:** Android emulator can't reach host localhost\n\n**Solution:** Use different IP\n\n```dart\n// For Android emulator\nstatic const String baseUrl = 'http://10.0.2.2:5000/api/v1';\n\n// For physical device (find your machine IP)\n// ifconfig | grep inet\n// Example: 192.168.1.100\nstatic const String baseUrl = 'http://192.168.1.100:5000/api/v1';\n```\n\n### Socket.IO not connecting\n\n**Error:** Messages don't appear, Socket.IO fails\n\n**Solution:** Check backend Socket.IO setup\n\n```bash\n# Check if Socket.IO is initialized\n# Look in backend logs for:\n# ✅ Socket.IO configured\n# ✅ Socket.IO event handlers registered\n\n# If missing: Implement Socket.IO in backend\n```\n\n---\n\n## 📊 API Endpoints Reference\n\nAll endpoints are now at: `http://localhost:5000/api/v1`\n\n### Authentication\n- `POST /auth/login` - Login user\n- `POST /auth/register` - Register new user\n- `GET /auth/me` - Get current user\n- `POST /auth/logout` - Logout user\n\n### Focus Timer\n- `POST /focus` - Start new session\n- `GET /focus/active` - Get active session\n- `POST /focus/:id/pause` - Pause session\n- `POST /focus/:id/resume` - Resume session\n- `POST /focus/:id/stop` - Stop session\n- `GET /focus/stats/today` - Get daily stats\n\n### Community\n- `GET /community/posts` - Get all posts\n- `POST /community/messages` - Send message\n- `POST /community/posts/:id/comments` - Add comment\n- Socket.IO: `chat:message` event\n- Socket.IO: `post:comment` event\n\n### Analytics\n- `GET /analytics/my-rank` - Get user rank and stats\n- `GET /analytics/leaderboard` - Get leaderboard\n- `GET /analytics/insights` - Get insights\n\n### Dashboard\n- `GET /dashboard?userId=...` - Get dashboard data (MUST pass userId!)\n- `GET /content/quote/today` - Get daily quote\n\n### Uploads\n- `POST /uploads/avatar` - Upload avatar\n- `POST /uploads/community` - Upload community media\n\n---\n\n## 🔐 Security Notes for Development\n\n**Current setup is development-only:**\n\n```env\nJWT_SECRET=dev-jwt-secret-key-for-testing-only-change-in-production\nCORS_ORIGIN=*  # Allows all origins\n```\n\n**For production:**\n\n```env\nJWT_SECRET=your-strong-secret-key-here-change-this\nCORS_ORIGIN=https://yourdomain.com\nNODE_ENV=production\n```\n\n---\n\n## 📝 Testing Checklist\n\n### Pre-Test\n- [ ] Backend running (`npm run dev`)\n- [ ] MongoDB connected\n- [ ] App updated to use `http://localhost:5000/api/v1`\n- [ ] Flutter dependencies fresh (`flutter clean && flutter pub get`)\n\n### Functional Tests\n- [ ] Focus timer counts down\n- [ ] Community posts load\n- [ ] Can send messages in real-time\n- [ ] Can add comments\n- [ ] Profile shows real analytics\n- [ ] Dashboard shows real data\n- [ ] Images upload successfully\n\n### Edge Cases\n- [ ] Network error handling works\n- [ ] Loading states show properly\n- [ ] Error messages display\n- [ ] Session persists (restart app)\n- [ ] Multiple users can interact\n\n---\n\n## 🚀 Deployment (Production)\n\n### When ready for production:\n\n1. **Update API endpoint to Render:**\n   ```dart\n   static const String baseUrl = 'https://flutter-app-v2.onrender.com/api/v1';\n   ```\n\n2. **Update environment variables:**\n   ```env\n   NODE_ENV=production\n   JWT_SECRET=your-production-secret\n   CORS_ORIGIN=https://yourdomain.com\n   ```\n\n3. **Deploy backend to Render:**\n   ```bash\n   git push  # Triggers automatic deploy\n   ```\n\n4. **Build Flutter app for production:**\n   ```bash\n   flutter build apk --release\n   # or\n   flutter build ios --release\n   ```\n\n---\n\n## 📞 Common Issues & Solutions\n\n| Issue | Cause | Solution |\n|-------|-------|----------|\n| App shows loading spinner | Backend not responding | Start backend (`npm run dev`) |\n| Timer won't start | Focus endpoints missing | Implement POST /focus endpoint |\n| Community posts don't load | GET /community/posts fails | Implement endpoint |\n| Messages don't send | Socket.IO not configured | Setup Socket.IO on backend |\n| Profile shows #12, 142h, 45 | Analytics endpoint missing | Implement GET /analytics/my-rank |\n| Dashboard shows no data | userId not passed | Check backend receives userId param |\n| Images won't upload | Upload endpoints missing | Implement POST /uploads/* endpoints |\n| Emulator can't reach localhost | Network isolation | Use http://10.0.2.2:5000 instead |\n\n---\n\n## ✅ Status\n\n**Backend:** ✅ Running locally on http://localhost:5000  \n**App:** ✅ Configured to use local backend  \n**Database:** ✅ MongoDB Atlas connected  \n**Ready:** ✅ All systems go!  \n\n---\n\n**Next Step:** Run `npm run dev` in backend folder, then `flutter run` in app folder\n\nEverything should work now! 🎉\n"